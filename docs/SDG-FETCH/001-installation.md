# Installation

```sh
sdgpkg install sdg-fetch
```

This installs the `sdgfetch` command (both display and configuration via `sdgfetch config`), copies logo files and presets, and places documentation and tips on your system. SDG-FETCH is also installed automatically as part of SDG-TERM.
