# Installation

```sh
sdgpkg install sdg-fetch
```

This installs the `sdgfetch` and `sdgfetch-conf` commands, copies logo files and presets, and places documentation and tips on your system. SDG-FETCH is also installed automatically as part of SDG-TERM.
