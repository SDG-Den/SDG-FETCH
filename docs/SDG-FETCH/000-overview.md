# Overview

SDG-FETCH displays your system information (OS, kernel, CPU, GPU, memory, and more) together with a decorative ASCII art logo. You can choose from a large library of logos and customize the display style through presets.

It runs automatically when you open a terminal, and you can reconfigure the logo and layout at any time through the interactive picker (`sdgfetch config`).
