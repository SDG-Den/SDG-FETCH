# Test Checklist — SDG-FETCH

## Commands
- [ ] `sdgfetch` — displays system info with logo
- [ ] `sdgfetch <logoname>` — uses specified logo (grep matching)
- [ ] `sdgfetch setconf <configname>` — uses specified preset (grep matching)
- [ ] `sdgfetch config` — opens interactive configurator (fzf picker)
- [ ] All logo presets render without errors
- [ ] All display presets render without errors
- [ ] Runs on terminal startup without errors
